# Bug Log

Real issues I hit while building these workflows, what caused them, and how I fixed them. Kept separate from notes because these are the things that actually taught me how n8n works under the hood.

---

## 001 — Unrecognized node type: n8n-nodes-tallyforms.tallyTrigger

**Context:** Imported a workflow JSON that used a Tally Forms trigger node.

**Error:**
```
Unrecognized node type: n8n-nodes-tallyforms.tallyTrigger
```

**Cause:** `n8n-nodes-tallyforms` is a community node — an npm package, not part of n8n's core/default node set. Community nodes can only be installed on **self-hosted** n8n instances. **n8n Cloud does not support installing community nodes at all**, so any workflow JSON referencing one will fail to load properly on Cloud, regardless of whether the node "exists" elsewhere.

**Fix:** Replaced the Tally Trigger node with n8n's native **Webhook** node (built-in, Cloud-compatible). Tally supports outgoing webhooks natively (Connect → Webhooks in Tally's form settings) — pointed Tally at the URL generated by the n8n Webhook node. Functionally equivalent to the dedicated node; the only loss is the friendly field-mapping UI the dedicated node provides (you work with raw JSON instead).

**Lesson:** Community nodes = self-hosted only. Always check whether a course/template was built on self-hosted before assuming a node will work on Cloud.

---

## 002 — LLM JSON output not splitting into separate fields

**Context:** Prompted Google Gemini to respond in strict JSON format (`{"Subject": "", "Icebreaker": "", ...}`), expecting the output to map directly to separate fields for a Google Sheets update.

**Symptom:** Output showed the full Gemini API response object (`content.parts[0].text`, `role`, `finishReason`, `index`) — and the JSON I asked for was sitting as one long unparsed string inside `parts[0].text`, not as separate accessible fields.

**Cause:** This wasn't actually a bug — it was a misunderstanding of how LLM responses work. The model returning "JSON" doesn't mean n8n automatically treats it as structured data. The model returns **text** that happens to follow JSON syntax. Nothing tells n8n to interpret that string as an object until an explicit parsing step is added.

**Fix:** Two valid approaches, used the Code node short-term:

```javascript
// Run Once for All Items (initial, buggy version — see bug 003)
const rawText = $input.first().json.content.parts[0].text;
const cleaned = rawText.replace(/```json|```/g, '').trim();
const parsed = JSON.parse(cleaned);
return [{ json: parsed }];
```

Longer-term, switched to n8n's native **Structured Output Parser** node (connects to the AI node's dedicated Output Parser input), which handles parsing + schema enforcement + retries on malformed output — more robust than manual `JSON.parse()`.

**Lesson:** "Respond in JSON" is a prompting instruction, not a data transformation. Parsing is always a separate, explicit step.

---

## 003 — Only first row updating in Google Sheets despite 3 input items

**Context:** Workflow: Get rows (3 items) → Gemini (3 items) → Code node (parses JSON) → Update Google Sheet. Only row 1 was being updated; rows 2 and 3 were left blank.

**Symptom:** Code node's output panel showed **1 item** despite **3 items** entering the node.

**Cause:** The Code node's **Mode** was set to `Run Once for All Items` instead of `Run Once for Each Item`. In "Run Once for All Items" mode, the script body executes exactly once total, and `$input.first()` always grabs only item 0 — items 1 and 2 were silently ignored. The `return [{ json: parsed }]` line then only ever returned a single-item array.

**Fix:** Changed Mode to `Run Once for Each Item`. Updated code to match per-item execution context:

```javascript
// Run Once for Each Item (fixed)
const rawText = $json.content.parts[0].text;
const cleaned = rawText.replace(/```json|```/g, '').trim();
const parsed = JSON.parse(cleaned);
return { json: parsed };
```

Key differences in per-item mode:
- Use `$json` (current item) instead of `$input.first()` (always item 0)
- Return a single object `{ json: parsed }`, not an array `[{ json: parsed }]`

**Lesson:** Always check the Mode dropdown on Code nodes when item counts don't match expectations going in vs. coming out. "Run Once for All Items" vs "Run Once for Each Item" fundamentally changes both execution behavior and the correct syntax for accessing/returning data.

---

## 004 — AI Agent claims it has no tool access, despite a working tool being connected

**Context:** Built an AI Agent-based chat assistant with a "Get many events in Google Calendar" tool connected, to answer questions like "what am I doing on 24 June 2026."

**Symptom:** Agent replied: *"I am sorry, but I cannot tell you what you are doing on June 24, 2026. I do not have access to your personal calendar information to look up specific events."* — despite the Calendar tool being correctly configured and connected.

**Diagnosis:** Checked the Logs panel after execution. The chain went straight from Memory/Model to the final response — the Calendar tool never appeared as an executed step. The model wasn't failing to use the tool; it wasn't attempting to use it at all.

**Cause:** No system prompt was set on the AI Agent. Without explicit instructions, the model defaults to a strongly learned training pattern: "as an AI, I don't have access to your personal data/calendar." Having a tool wired up structurally doesn't override that default refusal behavior — the model has to be told, in-context, that this specific assistant has real tool access and is expected to use it.

**Fix:** Added a system prompt explicitly countering the default refusal pattern, e.g.:
```
You are a helpful calendar assistant with real, working access to the
user's Google Calendar through your tools. You are NOT a generic AI
without calendar access — you have a "Get many events" tool that
actually queries their real calendar data. When the user asks about
their schedule, you MUST use the tool to check before answering.
```
After this, the Logs panel showed the Calendar tool as an actual executed step, and the agent correctly returned real event data.

**Lesson:** For AI Agents specifically, a missing system prompt can cause *silent* failures — not errors, but confidently wrong refusals — because the model falls back on default "I can't access that" language baked in from general training. Always explicitly state that the agent has real tool access and should use it, especially for data categories (personal info, calendars, files) where models have strong learned habits of declining by default. The Logs panel — not just the final chat output — is the right place to check whether a tool was actually invoked or silently skipped.

---

## Template for new entries

```markdown
## 00X — Short description

**Context:** What were you doing / building?

**Error / Symptom:** Exact error message or unexpected behavior.

**Cause:** Root cause, once understood.

**Fix:** What you changed, with code/config if relevant.

**Lesson:** One-line takeaway for future you.
```
